# Project Context
> Auto-maintained by AI. You can edit this file freely.
> Last updated: 2026-05-16

## Stack
- Go workspace: services/core, apps/cli
- Go SDK: packages/sdk-go
- Backend: chi, SQLite, goose, SSE, slog
- CLI/TUI: cobra + Bubble Tea/Lip Gloss/Bubbles; terminal-first target
- Legacy/optional frontend: Next.js app still present under apps/web

## Architecture Decisions
- Product command/binary: `zero`
- Direction: terminal-first, SDK-first; web must not be primary
- CLI lifecycle: `zero setup/start/stop/restart/status/logs` manage local daemon
- TUI: default `zero` opens OpenCode-like Bubble Tea UI; `zero -p` remains non-interactive
- Provider default: 9router-compatible OpenAI API at localhost:20128
- Default model: `cx/gpt-5.5` because local 9router responds quickly; avoid stale `kr/claude-sonnet-4.5` sessions

## Conventions
- Keep program runnable after each step
- Tools must stay project-root scoped and permission-gated when dangerous
- No TanStack

## Current Status
- [ ] Verify SDK-first CLI + daemon lifecycle commands ← IN PROGRESS

## Important Notes
- Latest CLI root can run interactive/non-interactive and starts embedded server
- CLI supported commands use `packages/sdk-go` instead of owning raw REST transport
- Provider fix: OpenAI-compatible 9router must receive full model IDs like `kr/claude-sonnet-4.5`; do not strip prefixes
- apps/web still exists as legacy/optional
